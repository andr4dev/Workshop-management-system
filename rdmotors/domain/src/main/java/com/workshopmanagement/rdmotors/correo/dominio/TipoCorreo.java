package com.workshopmanagement.rdmotors.correo.dominio;

/** Qué correo es (spec 0010). */
public enum TipoCorreo {
    /** El resumen de un turno cerrado. */
    CIERRE_DE_TURNO,
    /** El que pide el administrador para ver que la cuenta de Brevo funciona. */
    PRUEBA
}
